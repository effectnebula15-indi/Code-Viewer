package com.codeviewer.app.syntax

enum class TokenType {
    KEYWORD,
    STRING,
    COMMENT,
    NUMBER,
    TYPE,
    FUNCTION,
    OPERATOR,
    ANNOTATION,
    TAG,
    ATTRIBUTE,
    PUNCTUATION,
    PLAIN
}
